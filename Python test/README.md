Before runing the program, install the following dependencies:

`pip install peewee`

---

After installation, open ***test.py*** and run

Alternatively, with Python Test as the main directory, run the command

`python test.py`
